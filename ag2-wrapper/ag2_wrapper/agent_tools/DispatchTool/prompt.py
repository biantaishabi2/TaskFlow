"""
DispatchTool 的提示词配置
"""

PROMPT = """这是一个任务分发调度工具，用于协调和分发任务给其他工具执行。

使用此工具前请注意：

1. 任务描述：
   - 提供详细的任务描述，包括需要执行的操作和预期结果
   - 任务描述应当完整且自主，无需用户进一步干预

2. 工具使用限制：
   - 默认只能使用只读工具（如FileReadTool、GlobTool等）
   - 不能使用修改类工具（如BashTool、FileWriteTool等），这些工具需要直接调用
   - 不能递归调用DispatchTool自身

3. 并发执行：
   - 支持并发执行多个任务以提高性能
   - 适合需要多工具协作的复杂任务

4. 结果处理：
   - 会汇总所有执行结果并返回
   - 提供执行统计信息（工具使用次数、执行时间等）
   
5. 任务完成：
   - 完成任务后，请明确指出任务已完成，并详细总结你的发现和结果
   - 不需要特殊的工具来标记任务结束"""

DESCRIPTION = "任务分发调度工具，用于协调和分发任务给其他工具执行"