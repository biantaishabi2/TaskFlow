"""
GlobTool 的提示词配置
"""

TOOL_NAME_FOR_PROMPT = "GlobTool"

DESCRIPTION = """- 快速的文件模式匹配工具，适用于任意大小的代码库
- 支持 glob 模式，如 "**/*.js" 或 "src/**/*.ts"
- 返回按修改时间排序的匹配文件路径
- 当需要通过文件名模式查找文件时使用此工具
- 如果需要进行多轮 glob 和 grep 的开放式搜索，请使用 Agent 工具
"""

PROMPT = """使用 glob 模式快速搜索文件。

使用此工具前请注意：

1. 支持标准的 glob 模式：
   - 使用 "**/*.js" 匹配所有 JS 文件
   - 使用 "src/**/*.ts" 匹配 src 目录下的所有 TS 文件
   - 不支持大括号展开语法（如 "*.{js,ts}"）
   - 如需匹配多种文件类型，请使用多次搜索

2. 搜索结果：
   - 默认限制返回 100 个文件
   - 按修改时间排序（最新的在前）
   - 返回相对路径（除非指定 verbose 模式）
   - 提供执行时间和文件数统计

3. 性能考虑：
   - 适用于任意大小的代码库
   - 支持分页加载（使用 limit 和 offset）
   - 超出限制时提供截断提示
   - 建议使用更具体的路径或模式缩小范围

4. 常见模式示例：
   - "*.py" - 当前目录下的 Python 文件
   - "**/*.md" - 所有 Markdown 文件
   - "src/**/*.js" - src 目录下的所有 JS 文件
   - "test/test_*.py" - test 目录下的测试文件
   - "docs/*.{md,rst}" - 不支持此类大括号展开语法""" 