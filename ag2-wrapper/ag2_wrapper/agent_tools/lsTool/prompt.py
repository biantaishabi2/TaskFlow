"""
lsTool 的提示词配置
"""

PROMPT = """列出指定目录中的文件和文件夹。

使用此工具前请注意：

1. 应该优先使用 Glob 和 Grep 工具进行更专业的搜索

2. 目录限制：
   - 每次调用最多返回 1000 个文件
   - 超出限制时会被截断并提示
   - 建议使用更具体的路径参数

3. 路径使用说明：
   - 必须使用绝对路径，不要使用相对路径
   - 绝对路径以根目录(/)开始
   - 工具会自动跳过隐藏文件和目录
   - 参数格式: {"params": {"path": "/absolute/path/to/dir"}}"""

DESCRIPTION = "以树状结构列出指定目录的内容（文件和子目录），要求使用绝对路径" 